fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.0.0
soundfile>=0.12.1
numpy>=1.24.0
omnivoice>=0.1.5
# omnivoice-triton requires triton>=3.4.0, but torch 2.5.1+cu121 pins triton==3.1.0.
# To bypass this conflict, install it from the fork separately:
# pip install git+https://github.com/entermask/omnivoice-triton.git --no-deps
# Or for local development, install in editable mode:
# pip install -e /Users/thinhj/omnivoice-triton --no-deps
omnivoice-triton @ git+https://github.com/entermask/omnivoice-triton.git
faster-whisper>=1.0.0
nvidia-cublas-cu12>=12.1.3.1
nvidia-cudnn-cu12>=8.9.2.26
